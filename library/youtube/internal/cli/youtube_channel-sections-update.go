// Copyright 2026 peter-moelzer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newYoutubeChannelSectionsUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagPart string
	var flagOnBehalfOfContentOwner string
	var bodyContentDetailsChannels string
	var bodyContentDetailsPlaylists string
	var bodyEtag string
	var bodyId string
	var bodyKind string
	var bodyLocalizations string
	var bodySnippetChannelId string
	var bodySnippetDefaultLanguage string
	var bodySnippetLocalizedTitle string
	var bodySnippetPosition int
	var bodySnippetStyle string
	var bodySnippetTitle string
	var bodySnippetType string
	var bodyTargetingCountries string
	var bodyTargetingLanguages string
	var bodyTargetingRegions string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "channel-sections-update",
		Short:       "Updates an existing resource.",
		Example:     "  youtube-pp-cli youtube channel-sections-update --part example-value",
		Annotations: map[string]string{"pp:endpoint": "youtube.channel-sections-update", "pp:method": "PUT", "pp:path": "/youtube/v3/channelSections"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("part") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "part")
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/youtube/v3/channelSections"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedContentDetails := map[string]any{}
					if bodyContentDetailsChannels != "" {
						var parsedContentDetailsChannels any
						if err := json.Unmarshal([]byte(bodyContentDetailsChannels), &parsedContentDetailsChannels); err != nil {
							return fmt.Errorf("parsing --content-details-channels JSON: %w", err)
						}
						nestedContentDetails["channels"] = parsedContentDetailsChannels
					}
					if bodyContentDetailsPlaylists != "" {
						var parsedContentDetailsPlaylists any
						if err := json.Unmarshal([]byte(bodyContentDetailsPlaylists), &parsedContentDetailsPlaylists); err != nil {
							return fmt.Errorf("parsing --content-details-playlists JSON: %w", err)
						}
						nestedContentDetails["playlists"] = parsedContentDetailsPlaylists
					}
					if len(nestedContentDetails) > 0 {
						body["contentDetails"] = nestedContentDetails
					}
				}
				if bodyEtag != "" {
					body["etag"] = bodyEtag
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyKind != "" {
					body["kind"] = bodyKind
				}
				if bodyLocalizations != "" {
					var parsedLocalizations any
					if err := json.Unmarshal([]byte(bodyLocalizations), &parsedLocalizations); err != nil {
						return fmt.Errorf("parsing --localizations JSON: %w", err)
					}
					body["localizations"] = parsedLocalizations
				}
				{
					nestedSnippet := map[string]any{}
					if bodySnippetChannelId != "" {
						nestedSnippet["channelId"] = bodySnippetChannelId
					}
					if bodySnippetDefaultLanguage != "" {
						nestedSnippet["defaultLanguage"] = bodySnippetDefaultLanguage
					}
					{
						nestedSnippetLocalized := map[string]any{}
						if bodySnippetLocalizedTitle != "" {
							nestedSnippetLocalized["title"] = bodySnippetLocalizedTitle
						}
						if len(nestedSnippetLocalized) > 0 {
							nestedSnippet["localized"] = nestedSnippetLocalized
						}
					}
					if bodySnippetPosition != 0 {
						nestedSnippet["position"] = bodySnippetPosition
					}
					if bodySnippetStyle != "" {
						nestedSnippet["style"] = bodySnippetStyle
					}
					if bodySnippetTitle != "" {
						nestedSnippet["title"] = bodySnippetTitle
					}
					if bodySnippetType != "" {
						nestedSnippet["type"] = bodySnippetType
					}
					if len(nestedSnippet) > 0 {
						body["snippet"] = nestedSnippet
					}
				}
				{
					nestedTargeting := map[string]any{}
					if bodyTargetingCountries != "" {
						var parsedTargetingCountries any
						if err := json.Unmarshal([]byte(bodyTargetingCountries), &parsedTargetingCountries); err != nil {
							return fmt.Errorf("parsing --targeting-countries JSON: %w", err)
						}
						nestedTargeting["countries"] = parsedTargetingCountries
					}
					if bodyTargetingLanguages != "" {
						var parsedTargetingLanguages any
						if err := json.Unmarshal([]byte(bodyTargetingLanguages), &parsedTargetingLanguages); err != nil {
							return fmt.Errorf("parsing --targeting-languages JSON: %w", err)
						}
						nestedTargeting["languages"] = parsedTargetingLanguages
					}
					if bodyTargetingRegions != "" {
						var parsedTargetingRegions any
						if err := json.Unmarshal([]byte(bodyTargetingRegions), &parsedTargetingRegions); err != nil {
							return fmt.Errorf("parsing --targeting-regions JSON: %w", err)
						}
						nestedTargeting["regions"] = parsedTargetingRegions
					}
					if len(nestedTargeting) > 0 {
						body["targeting"] = nestedTargeting
					}
				}
			}
			params := map[string]string{}
			if flagPart != "" {
				params["part"] = fmt.Sprintf("%v", flagPart)
			}
			if flagOnBehalfOfContentOwner != "" {
				params["onBehalfOfContentOwner"] = fmt.Sprintf("%v", flagOnBehalfOfContentOwner)
			}
			data, statusCode, err := c.PutWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "youtube",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPart, "part", "", "The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation...")
	cmd.Flags().StringVar(&flagOnBehalfOfContentOwner, "on-behalf-of-content-owner", "", "*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter...")
	cmd.Flags().StringVar(&bodyContentDetailsChannels, "content-details-channels", "", "The channel ids for type multiple_channels.")
	cmd.Flags().StringVar(&bodyContentDetailsPlaylists, "content-details-playlists", "", "The playlist ids for type single_playlist and multiple_playlists. For singlePlaylist, only one playlistId is allowed.")
	cmd.Flags().StringVar(&bodyEtag, "etag", "", "Etag of this resource.")
	cmd.Flags().StringVar(&bodyId, "id", "", "The ID that YouTube uses to uniquely identify the channel section.")
	cmd.Flags().StringVar(&bodyKind, "kind", "youtube#channelSection", "Identifies what kind of resource this is. Value: the fixed string 'youtube#channelSection'.")
	cmd.Flags().StringVar(&bodyLocalizations, "localizations", "", "Localizations for different languages")
	cmd.Flags().StringVar(&bodySnippetChannelId, "snippet-channel-id", "", "The ID that YouTube uses to uniquely identify the channel that published the channel section.")
	cmd.Flags().StringVar(&bodySnippetDefaultLanguage, "snippet-default-language", "", "The language of the channel section's default title and description.")
	cmd.Flags().StringVar(&bodySnippetLocalizedTitle, "snippet-localized-title", "", "The localized strings for channel section's title.")
	cmd.Flags().IntVar(&bodySnippetPosition, "snippet-position", 0, "The position of the channel section in the channel.")
	cmd.Flags().StringVar(&bodySnippetStyle, "snippet-style", "", "The style of the channel section.")
	cmd.Flags().StringVar(&bodySnippetTitle, "snippet-title", "", "The channel section's title for multiple_playlists and multiple_channels.")
	cmd.Flags().StringVar(&bodySnippetType, "snippet-type", "", "The type of the channel section.")
	cmd.Flags().StringVar(&bodyTargetingCountries, "targeting-countries", "", "The country the channel section is targeting.")
	cmd.Flags().StringVar(&bodyTargetingLanguages, "targeting-languages", "", "The language the channel section is targeting.")
	cmd.Flags().StringVar(&bodyTargetingRegions, "targeting-regions", "", "The region the channel section is targeting.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
