// Copyright 2026 peter-moelzer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newYoutubeThirdPartyLinksUpdateCmd(flags *rootFlags) *cobra.Command {
	var flagPart string
	var flagExternalChannelId string
	var bodyEtag string
	var bodyKind string
	var bodyLinkingToken string
	var bodySnippetChannelToStoreLinkMerchantId string
	var bodySnippetChannelToStoreLinkStoreName string
	var bodySnippetChannelToStoreLinkStoreUrl string
	var bodySnippetType string
	var bodyStatusLinkStatus string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "third-party-links-update",
		Short:       "Updates an existing resource.",
		Example:     "  youtube-pp-cli youtube third-party-links-update --part example-value",
		Annotations: map[string]string{"pp:endpoint": "youtube.third-party-links-update", "pp:method": "PUT", "pp:path": "/youtube/v3/thirdPartyLinks"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("part") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "part")
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/youtube/v3/thirdPartyLinks"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyEtag != "" {
					body["etag"] = bodyEtag
				}
				if bodyKind != "" {
					body["kind"] = bodyKind
				}
				if bodyLinkingToken != "" {
					body["linkingToken"] = bodyLinkingToken
				}
				{
					nestedSnippet := map[string]any{}
					{
						nestedSnippetChannelToStoreLink := map[string]any{}
						if bodySnippetChannelToStoreLinkMerchantId != "" {
							nestedSnippetChannelToStoreLink["merchantId"] = bodySnippetChannelToStoreLinkMerchantId
						}
						if bodySnippetChannelToStoreLinkStoreName != "" {
							nestedSnippetChannelToStoreLink["storeName"] = bodySnippetChannelToStoreLinkStoreName
						}
						if bodySnippetChannelToStoreLinkStoreUrl != "" {
							nestedSnippetChannelToStoreLink["storeUrl"] = bodySnippetChannelToStoreLinkStoreUrl
						}
						if len(nestedSnippetChannelToStoreLink) > 0 {
							nestedSnippet["channelToStoreLink"] = nestedSnippetChannelToStoreLink
						}
					}
					if bodySnippetType != "" {
						nestedSnippet["type"] = bodySnippetType
					}
					if len(nestedSnippet) > 0 {
						body["snippet"] = nestedSnippet
					}
				}
				{
					nestedStatus := map[string]any{}
					if bodyStatusLinkStatus != "" {
						nestedStatus["linkStatus"] = bodyStatusLinkStatus
					}
					if len(nestedStatus) > 0 {
						body["status"] = nestedStatus
					}
				}
			}
			params := map[string]string{}
			if flagPart != "" {
				params["part"] = fmt.Sprintf("%v", flagPart)
			}
			if flagExternalChannelId != "" {
				params["externalChannelId"] = fmt.Sprintf("%v", flagExternalChannelId)
			}
			data, statusCode, err := c.PutWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "youtube",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPart, "part", "", "The *part* parameter specifies the thirdPartyLink resource parts that the API request and response will include....")
	cmd.Flags().StringVar(&flagExternalChannelId, "external-channel-id", "", "Channel ID to which changes should be applied, for delegation.")
	cmd.Flags().StringVar(&bodyEtag, "etag", "", "Etag of this resource")
	cmd.Flags().StringVar(&bodyKind, "kind", "youtube#thirdPartyLink", "Identifies what kind of resource this is. Value: the fixed string 'youtube#thirdPartyLink'.")
	cmd.Flags().StringVar(&bodyLinkingToken, "linking-token", "", "The linking_token identifies a YouTube account and channel with which the third party account is linked.")
	cmd.Flags().StringVar(&bodySnippetChannelToStoreLinkMerchantId, "snippet-channel-to-store-link-merchant-id", "", "Google Merchant Center id of the store.")
	cmd.Flags().StringVar(&bodySnippetChannelToStoreLinkStoreName, "snippet-channel-to-store-link-store-name", "", "Name of the store.")
	cmd.Flags().StringVar(&bodySnippetChannelToStoreLinkStoreUrl, "snippet-channel-to-store-link-store-url", "", "Landing page of the store.")
	cmd.Flags().StringVar(&bodySnippetType, "snippet-type", "", "Type of the link named after the entities that are being linked.")
	cmd.Flags().StringVar(&bodyStatusLinkStatus, "status-link-status", "", "Link status")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
