// Copyright 2026 peter-moelzer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newYoutubeCommentsInsertCmd(flags *rootFlags) *cobra.Command {
	var flagPart string
	var bodyEtag string
	var bodyId string
	var bodyKind string
	var bodySnippetAuthorChannelIdValue string
	var bodySnippetAuthorChannelUrl string
	var bodySnippetAuthorDisplayName string
	var bodySnippetAuthorProfileImageUrl string
	var bodySnippetCanRate bool
	var bodySnippetChannelId string
	var bodySnippetLikeCount int
	var bodySnippetModerationStatus string
	var bodySnippetParentId string
	var bodySnippetPublishedAt string
	var bodySnippetTextDisplay string
	var bodySnippetTextOriginal string
	var bodySnippetUpdatedAt string
	var bodySnippetVideoId string
	var bodySnippetViewerRating string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "comments-insert",
		Short:       "Inserts a new resource into this collection.",
		Example:     "  youtube-pp-cli youtube comments-insert --part example-value",
		Annotations: map[string]string{"pp:endpoint": "youtube.comments-insert", "pp:method": "POST", "pp:path": "/youtube/v3/comments"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("part") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "part")
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/youtube/v3/comments"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyEtag != "" {
					body["etag"] = bodyEtag
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyKind != "" {
					body["kind"] = bodyKind
				}
				{
					nestedSnippet := map[string]any{}
					{
						nestedSnippetAuthorChannelId := map[string]any{}
						if bodySnippetAuthorChannelIdValue != "" {
							nestedSnippetAuthorChannelId["value"] = bodySnippetAuthorChannelIdValue
						}
						if len(nestedSnippetAuthorChannelId) > 0 {
							nestedSnippet["authorChannelId"] = nestedSnippetAuthorChannelId
						}
					}
					if bodySnippetAuthorChannelUrl != "" {
						nestedSnippet["authorChannelUrl"] = bodySnippetAuthorChannelUrl
					}
					if bodySnippetAuthorDisplayName != "" {
						nestedSnippet["authorDisplayName"] = bodySnippetAuthorDisplayName
					}
					if bodySnippetAuthorProfileImageUrl != "" {
						nestedSnippet["authorProfileImageUrl"] = bodySnippetAuthorProfileImageUrl
					}
					if bodySnippetCanRate != false {
						nestedSnippet["canRate"] = bodySnippetCanRate
					}
					if bodySnippetChannelId != "" {
						nestedSnippet["channelId"] = bodySnippetChannelId
					}
					if bodySnippetLikeCount != 0 {
						nestedSnippet["likeCount"] = bodySnippetLikeCount
					}
					if bodySnippetModerationStatus != "" {
						nestedSnippet["moderationStatus"] = bodySnippetModerationStatus
					}
					if bodySnippetParentId != "" {
						nestedSnippet["parentId"] = bodySnippetParentId
					}
					if bodySnippetPublishedAt != "" {
						nestedSnippet["publishedAt"] = bodySnippetPublishedAt
					}
					if bodySnippetTextDisplay != "" {
						nestedSnippet["textDisplay"] = bodySnippetTextDisplay
					}
					if bodySnippetTextOriginal != "" {
						nestedSnippet["textOriginal"] = bodySnippetTextOriginal
					}
					if bodySnippetUpdatedAt != "" {
						nestedSnippet["updatedAt"] = bodySnippetUpdatedAt
					}
					if bodySnippetVideoId != "" {
						nestedSnippet["videoId"] = bodySnippetVideoId
					}
					if bodySnippetViewerRating != "" {
						nestedSnippet["viewerRating"] = bodySnippetViewerRating
					}
					if len(nestedSnippet) > 0 {
						body["snippet"] = nestedSnippet
					}
				}
			}
			params := map[string]string{}
			if flagPart != "" {
				params["part"] = fmt.Sprintf("%v", flagPart)
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "youtube",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPart, "part", "", "The *part* parameter identifies the properties that the API response will include. Set the parameter value to...")
	cmd.Flags().StringVar(&bodyEtag, "etag", "", "Etag of this resource.")
	cmd.Flags().StringVar(&bodyId, "id", "", "The ID that YouTube uses to uniquely identify the comment.")
	cmd.Flags().StringVar(&bodyKind, "kind", "youtube#comment", "Identifies what kind of resource this is. Value: the fixed string 'youtube#comment'.")
	cmd.Flags().StringVar(&bodySnippetAuthorChannelIdValue, "snippet-author-channel-id-value", "", "Value")
	cmd.Flags().StringVar(&bodySnippetAuthorChannelUrl, "snippet-author-channel-url", "", "Link to the author's YouTube channel, if any.")
	cmd.Flags().StringVar(&bodySnippetAuthorDisplayName, "snippet-author-display-name", "", "The name of the user who posted the comment.")
	cmd.Flags().StringVar(&bodySnippetAuthorProfileImageUrl, "snippet-author-profile-image-url", "", "The URL for the avatar of the user who posted the comment.")
	cmd.Flags().BoolVar(&bodySnippetCanRate, "snippet-can-rate", false, "Whether the current viewer can rate this comment.")
	cmd.Flags().StringVar(&bodySnippetChannelId, "snippet-channel-id", "", "The id of the corresponding YouTube channel. In case of a channel comment this is the channel the comment refers to....")
	cmd.Flags().IntVar(&bodySnippetLikeCount, "snippet-like-count", 0, "The total number of likes this comment has received.")
	cmd.Flags().StringVar(&bodySnippetModerationStatus, "snippet-moderation-status", "", "The comment's moderation status. Will not be set if the comments were requested through the id filter.")
	cmd.Flags().StringVar(&bodySnippetParentId, "snippet-parent-id", "", "The unique id of the parent comment, only set for replies.")
	cmd.Flags().StringVar(&bodySnippetPublishedAt, "snippet-published-at", "", "The date and time when the comment was originally published.")
	cmd.Flags().StringVar(&bodySnippetTextDisplay, "snippet-text-display", "", "The comment's text. The format is either plain text or HTML dependent on what has been requested. Even the plain...")
	cmd.Flags().StringVar(&bodySnippetTextOriginal, "snippet-text-original", "", "The comment's original raw text as initially posted or last updated. The original text will only be returned if it...")
	cmd.Flags().StringVar(&bodySnippetUpdatedAt, "snippet-updated-at", "", "The date and time when the comment was last updated.")
	cmd.Flags().StringVar(&bodySnippetVideoId, "snippet-video-id", "", "The ID of the video the comment refers to, if any.")
	cmd.Flags().StringVar(&bodySnippetViewerRating, "snippet-viewer-rating", "", "The rating the viewer has given to this comment. For the time being this will never return RATE_TYPE_DISLIKE and...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
