// Copyright 2026 peter-moelzer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newYoutubeCaptionsInsertCmd(flags *rootFlags) *cobra.Command {
	var flagPart string
	var flagOnBehalfOf string
	var flagOnBehalfOfContentOwner string
	var flagSync bool
	var bodyEtag string
	var bodyId string
	var bodyKind string
	var bodySnippetAudioTrackType string
	var bodySnippetFailureReason string
	var bodySnippetIsAutoSynced bool
	var bodySnippetIsCC bool
	var bodySnippetIsDraft bool
	var bodySnippetIsEasyReader bool
	var bodySnippetIsLarge bool
	var bodySnippetLanguage string
	var bodySnippetLastUpdated string
	var bodySnippetName string
	var bodySnippetStatus string
	var bodySnippetTrackKind string
	var bodySnippetVideoId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "captions-insert",
		Short:       "Inserts a new resource into this collection.",
		Example:     "  youtube-pp-cli youtube captions-insert --part example-value",
		Annotations: map[string]string{"pp:endpoint": "youtube.captions-insert", "pp:method": "POST", "pp:path": "/youtube/v3/captions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("part") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "part")
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/youtube/v3/captions"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyEtag != "" {
					body["etag"] = bodyEtag
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyKind != "" {
					body["kind"] = bodyKind
				}
				{
					nestedSnippet := map[string]any{}
					if bodySnippetAudioTrackType != "" {
						nestedSnippet["audioTrackType"] = bodySnippetAudioTrackType
					}
					if bodySnippetFailureReason != "" {
						nestedSnippet["failureReason"] = bodySnippetFailureReason
					}
					if bodySnippetIsAutoSynced != false {
						nestedSnippet["isAutoSynced"] = bodySnippetIsAutoSynced
					}
					if bodySnippetIsCC != false {
						nestedSnippet["isCC"] = bodySnippetIsCC
					}
					if bodySnippetIsDraft != false {
						nestedSnippet["isDraft"] = bodySnippetIsDraft
					}
					if bodySnippetIsEasyReader != false {
						nestedSnippet["isEasyReader"] = bodySnippetIsEasyReader
					}
					if bodySnippetIsLarge != false {
						nestedSnippet["isLarge"] = bodySnippetIsLarge
					}
					if bodySnippetLanguage != "" {
						nestedSnippet["language"] = bodySnippetLanguage
					}
					if bodySnippetLastUpdated != "" {
						nestedSnippet["lastUpdated"] = bodySnippetLastUpdated
					}
					if bodySnippetName != "" {
						nestedSnippet["name"] = bodySnippetName
					}
					if bodySnippetStatus != "" {
						nestedSnippet["status"] = bodySnippetStatus
					}
					if bodySnippetTrackKind != "" {
						nestedSnippet["trackKind"] = bodySnippetTrackKind
					}
					if bodySnippetVideoId != "" {
						nestedSnippet["videoId"] = bodySnippetVideoId
					}
					if len(nestedSnippet) > 0 {
						body["snippet"] = nestedSnippet
					}
				}
			}
			params := map[string]string{}
			if flagPart != "" {
				params["part"] = fmt.Sprintf("%v", flagPart)
			}
			if flagOnBehalfOf != "" {
				params["onBehalfOf"] = fmt.Sprintf("%v", flagOnBehalfOf)
			}
			if flagOnBehalfOfContentOwner != "" {
				params["onBehalfOfContentOwner"] = fmt.Sprintf("%v", flagOnBehalfOfContentOwner)
			}
			if flagSync != false {
				params["sync"] = fmt.Sprintf("%v", flagSync)
			}
			data, statusCode, err := c.PostWithParams(path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "youtube",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPart, "part", "", "The *part* parameter specifies the caption resource parts that the API response will include. Set the parameter...")
	cmd.Flags().StringVar(&flagOnBehalfOf, "on-behalf-of", "", "ID of the Google+ Page for the channel that the request is be on behalf of")
	cmd.Flags().StringVar(&flagOnBehalfOfContentOwner, "on-behalf-of-content-owner", "", "*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter...")
	cmd.Flags().BoolVar(&flagSync, "sync", false, "Extra parameter to allow automatically syncing the uploaded caption/transcript with the audio.")
	cmd.Flags().StringVar(&bodyEtag, "etag", "", "Etag of this resource.")
	cmd.Flags().StringVar(&bodyId, "id", "", "The ID that YouTube uses to uniquely identify the caption track.")
	cmd.Flags().StringVar(&bodyKind, "kind", "youtube#caption", "Identifies what kind of resource this is. Value: the fixed string 'youtube#caption'.")
	cmd.Flags().StringVar(&bodySnippetAudioTrackType, "snippet-audio-track-type", "", "The type of audio track associated with the caption track.")
	cmd.Flags().StringVar(&bodySnippetFailureReason, "snippet-failure-reason", "", "The reason that YouTube failed to process the caption track. This property is only present if the state property's...")
	cmd.Flags().BoolVar(&bodySnippetIsAutoSynced, "snippet-is-auto-synced", false, "Indicates whether YouTube synchronized the caption track to the audio track in the video. The value will be true if...")
	cmd.Flags().BoolVar(&bodySnippetIsCC, "snippet-is-cc", false, "Indicates whether the track contains closed captions for the deaf and hard of hearing. The default value is false.")
	cmd.Flags().BoolVar(&bodySnippetIsDraft, "snippet-is-draft", false, "Indicates whether the caption track is a draft. If the value is true, then the track is not publicly visible. The...")
	cmd.Flags().BoolVar(&bodySnippetIsEasyReader, "snippet-is-easy-reader", false, "Indicates whether caption track is formatted for 'easy reader,' meaning it is at a third-grade level for language...")
	cmd.Flags().BoolVar(&bodySnippetIsLarge, "snippet-is-large", false, "Indicates whether the caption track uses large text for the vision-impaired. The default value is false.")
	cmd.Flags().StringVar(&bodySnippetLanguage, "snippet-language", "", "The language of the caption track. The property value is a BCP-47 language tag.")
	cmd.Flags().StringVar(&bodySnippetLastUpdated, "snippet-last-updated", "", "The date and time when the caption track was last updated.")
	cmd.Flags().StringVar(&bodySnippetName, "snippet-name", "", "The name of the caption track. The name is intended to be visible to the user as an option during playback.")
	cmd.Flags().StringVar(&bodySnippetStatus, "snippet-status", "", "The caption track's status.")
	cmd.Flags().StringVar(&bodySnippetTrackKind, "snippet-track-kind", "", "The caption track's type.")
	cmd.Flags().StringVar(&bodySnippetVideoId, "snippet-video-id", "", "The ID that YouTube uses to uniquely identify the video associated with the caption track. @mutable...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
