// Copyright 2026 peter-moelzer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	// PATCH: Surface the local workflow that handles notification-like subscription uploads.
	// PATCH: Route "notifications" prompts to the fail-closed All-bell workflow alias.
	{Command: "workflow notifications", Description: "Find recent uploads from channels whose YouTube web bell menu is set to All and optionally add them to a playlist.", Group: "workflow", WhyItMatters: "Matches this workspace's meaning of notifications; the Data API does not expose bell-notification state, so the command requires an explicit channel-ID file."},
	{Command: "workflow recent-subscription-uploads", Description: "Find recent uploads from all subscribed channels and optionally add them to a playlist.", Group: "workflow", WhyItMatters: "Use only when the user asks for subscriptions broadly rather than YouTube notifications."},
	{Command: "youtube abuse-reports-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube activities-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube captions-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube captions-download", Description: "Downloads a caption track.", Group: "youtube"},
	{Command: "youtube captions-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube captions-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube captions-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube channel-banners-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube channel-sections-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube channel-sections-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube channel-sections-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube channel-sections-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube channels-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube channels-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube comment-threads-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube comment-threads-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube comments-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube comments-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube comments-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube comments-mark-as-spam", Description: "Expresses the caller's opinion that one or more comments should be flagged as spam.", Group: "youtube"},
	{Command: "youtube comments-set-moderation-status", Description: "Sets the moderation status of one or more comments.", Group: "youtube"},
	{Command: "youtube comments-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube i18n-languages-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube i18n-regions-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube live-broadcasts-bind", Description: "Bind a broadcast to a stream.", Group: "youtube"},
	{Command: "youtube live-broadcasts-delete", Description: "Delete a given broadcast.", Group: "youtube"},
	{Command: "youtube live-broadcasts-insert", Description: "Inserts a new stream for the authenticated user.", Group: "youtube"},
	{Command: "youtube live-broadcasts-insert-cuepoint", Description: "Insert cuepoints in a broadcast", Group: "youtube"},
	{Command: "youtube live-broadcasts-list", Description: "Retrieve the list of broadcasts associated with the given channel.", Group: "youtube"},
	{Command: "youtube live-broadcasts-transition", Description: "Transition a broadcast to a given status.", Group: "youtube"},
	{Command: "youtube live-broadcasts-update", Description: "Updates an existing broadcast for the authenticated user.", Group: "youtube"},
	{Command: "youtube live-chat-bans-delete", Description: "Deletes a chat ban.", Group: "youtube"},
	{Command: "youtube live-chat-bans-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube live-chat-messages-delete", Description: "Deletes a chat message.", Group: "youtube"},
	{Command: "youtube live-chat-messages-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube live-chat-messages-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube live-chat-moderators-delete", Description: "Deletes a chat moderator.", Group: "youtube"},
	{Command: "youtube live-chat-moderators-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube live-chat-moderators-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube live-streams-delete", Description: "Deletes an existing stream for the authenticated user.", Group: "youtube"},
	{Command: "youtube live-streams-insert", Description: "Inserts a new stream for the authenticated user.", Group: "youtube"},
	{Command: "youtube live-streams-list", Description: "Retrieve the list of streams associated with the given channel. --", Group: "youtube"},
	{Command: "youtube live-streams-update", Description: "Updates an existing stream for the authenticated user.", Group: "youtube"},
	{Command: "youtube members-list", Description: "Retrieves a list of members that match the request criteria for a channel.", Group: "youtube"},
	{Command: "youtube memberships-levels-list", Description: "Retrieves a list of all pricing levels offered by a creator to the fans.", Group: "youtube"},
	{Command: "youtube playlist-items-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube playlist-items-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube playlist-items-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube playlist-items-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube playlists-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube playlists-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube playlists-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube playlists-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube search-list", Description: "Retrieves a list of search resources", Group: "youtube"},
	{Command: "youtube subscriptions-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube subscriptions-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube subscriptions-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube super-chat-events-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube tests-insert", Description: "POST method.", Group: "youtube"},
	{Command: "youtube third-party-links-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube third-party-links-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube third-party-links-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube third-party-links-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube thumbnails-set", Description: "As this is not an insert in a strict sense (it supports uploading/setting of a thumbnail for multiple videos, which doesn't result in creation of a single resource), I use a custom verb here.", Group: "youtube"},
	{Command: "youtube update-comment-threads", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube video-abuse-report-reasons-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube video-categories-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube videos-delete", Description: "Deletes a resource.", Group: "youtube"},
	{Command: "youtube videos-get-rating", Description: "Retrieves the ratings that the authorized user gave to a list of specified videos.", Group: "youtube"},
	{Command: "youtube videos-insert", Description: "Inserts a new resource into this collection.", Group: "youtube"},
	{Command: "youtube videos-list", Description: "Retrieves a list of resources, possibly filtered.", Group: "youtube"},
	{Command: "youtube videos-rate", Description: "Adds a like or dislike rating to a video or removes a rating from a video.", Group: "youtube"},
	{Command: "youtube videos-report-abuse", Description: "Report abuse for a video.", Group: "youtube"},
	{Command: "youtube videos-update", Description: "Updates an existing resource.", Group: "youtube"},
	{Command: "youtube watermarks-set", Description: "Allows upload of watermark image and setting it for a channel.", Group: "youtube"},
	{Command: "youtube watermarks-unset", Description: "Allows removal of channel watermark.", Group: "youtube"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  youtube-pp-cli which "stale tickets"
  youtube-pp-cli which "bottleneck"
  youtube-pp-cli which --limit 1 "send message"
  youtube-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
