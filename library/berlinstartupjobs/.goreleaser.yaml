version: 2
project_name: berlinstartupjobs-pp-cli
changelog:
  disable: true
builds:
  - id: berlinstartupjobs-pp-cli
    main: ./cmd/berlinstartupjobs-pp-cli
    binary: berlinstartupjobs-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X berlinstartupjobs-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: berlinstartupjobs-pp-mcp
    main: ./cmd/berlinstartupjobs-pp-mcp
    binary: berlinstartupjobs-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: berlinstartupjobs-pp-cli
    repository:
      owner: peter-moelzer
      name: homebrew-tap
    homepage: "https://github.com/peter-moelzer/berlinstartupjobs-pp-cli"
    description: "Public HTML search pages for BerlinStartupJobs."
    install: |
      bin.install "berlinstartupjobs-pp-cli"
      bin.install "berlinstartupjobs-pp-mcp"
