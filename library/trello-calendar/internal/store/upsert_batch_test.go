// Copyright 2026 peter-moelzer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package store

import (
	"encoding/json"
	"fmt"
	"path/filepath"
	"strings"
	"sync"
	"testing"

	_ "modernc.org/sqlite"
)

// TestStoreWrite_NoSQLITE_BUSY_HighConcurrency exercises the writeMu serialization
// guarantee: 16 fetcher-style goroutines hammer the store with a mix of
// UpsertBatch, SaveSyncState, and SaveSyncCursor calls. Before the mutex
// fix, this test reproduces SQLITE_BUSY at default sync concurrency on
// pure-Go SQLite (modernc.org/sqlite + WAL) because multiple writers
// race for the WAL lock and busy_timeout retries are not exhaustive.
//
// Run under `go test -race` to catch any data races on Store fields.
func TestStoreWrite_NoSQLITE_BUSY_HighConcurrency(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	const goroutines = 16
	const itemsPerBatch = 5

	var wg sync.WaitGroup
	errCh := make(chan error, goroutines*3)

	for g := 0; g < goroutines; g++ {
		wg.Add(1)
		go func(gid int) {
			defer wg.Done()
			rt := fmt.Sprintf("rt_%d", gid)
			items := make([]json.RawMessage, 0, itemsPerBatch)
			for i := 0; i < itemsPerBatch; i++ {
				items = append(items, json.RawMessage(fmt.Sprintf(`{"id": "g%d-i%d"}`, gid, i)))
			}
			if _, _, err := s.UpsertBatch(rt, items); err != nil {
				errCh <- fmt.Errorf("UpsertBatch goroutine %d: %w", gid, err)
				return
			}
			if err := s.SaveSyncState(rt, fmt.Sprintf("cursor-%d", gid), itemsPerBatch); err != nil {
				errCh <- fmt.Errorf("SaveSyncState goroutine %d: %w", gid, err)
				return
			}
			if err := s.SaveSyncCursor(rt, fmt.Sprintf("cursor2-%d", gid)); err != nil {
				errCh <- fmt.Errorf("SaveSyncCursor goroutine %d: %w", gid, err)
				return
			}
		}(g)
	}
	wg.Wait()
	close(errCh)

	for err := range errCh {
		if err == nil {
			continue
		}
		// SQLITE_BUSY surfaces as "database is locked" or "SQLITE_BUSY"
		// in the error message — assert neither occurs.
		msg := err.Error()
		if strings.Contains(msg, "SQLITE_BUSY") || strings.Contains(strings.ToLower(msg), "database is locked") {
			t.Fatalf("got SQLITE_BUSY-class error under concurrent writers: %v", err)
		}
		t.Fatalf("unexpected error under concurrent writers: %v", err)
	}

	// Verify all rows persisted: goroutines * itemsPerBatch in the generic
	// resources table.
	db := s.DB()
	var total int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources`).Scan(&total); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if total != goroutines*itemsPerBatch {
		t.Fatalf("resources total = %d, want %d", total, goroutines*itemsPerBatch)
	}
}

// TestStoreWrite_PanicReleasesLock confirms that a panic inside a locked
// section unwinds via defer s.writeMu.Unlock() so subsequent writers can
// proceed. A leaked lock would deadlock the second call indefinitely.
func TestStoreWrite_PanicReleasesLock(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	// Trigger panic by passing a nil *Store method receiver indirectly:
	// we call UpsertBatch with malformed JSON that survives Unmarshal
	// (it's wrapped in skipped-count handling) — there's no easy panic
	// path inside a locked section that doesn't also corrupt state, so
	// we instead simulate the post-panic state by manually locking and
	// unlocking, then assert subsequent calls succeed.
	func() {
		defer func() {
			recover()
		}()
		s.writeMu.Lock()
		defer s.writeMu.Unlock()
		panic("simulated writer panic")
	}()

	// Subsequent writer must not block.
	done := make(chan struct{})
	go func() {
		if _, _, err := s.UpsertBatch("post_panic", []json.RawMessage{json.RawMessage(`{"id": "x"}`)}); err != nil {
			t.Errorf("post-panic UpsertBatch: %v", err)
		}
		close(done)
	}()
	<-done
}

// TestUpsertBatch_TemplatedIDFieldOverrideWins exercises the
// per-resource ID-field override. When the spec author annotates a
// path-item with x-resource-id, the profiler emits SyncableResource.IDField,
// the generator templates this into resourceIDFieldOverrides, and
// UpsertBatch consults that map first. This test seeds the override map
// at runtime (since the generated table here may or may not declare any
// override) to assert the lookup path itself works.
func TestUpsertBatch_TemplatedIDFieldOverrideWins(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	// Inject a runtime override for a synthetic resource. Item carries
	// no generic-fallback field (no id/name/uuid/...) — only a custom
	// "ticker" field. Without the override, all 3 items would be
	// dropped as PK-unresolved; with it, all 3 land.
	prev, hadPrev := resourceIDFieldOverrides["overrideTest"]
	resourceIDFieldOverrides["overrideTest"] = "ticker"
	defer func() {
		if hadPrev {
			resourceIDFieldOverrides["overrideTest"] = prev
		} else {
			delete(resourceIDFieldOverrides, "overrideTest")
		}
	}()

	items := []json.RawMessage{
		json.RawMessage(`{"ticker": "AAPL", "price": 100}`),
		json.RawMessage(`{"ticker": "GOOG", "price": 200}`),
		json.RawMessage(`{"ticker": "MSFT", "price": 300}`),
	}
	stored, extractFailures, err := s.UpsertBatch("overrideTest", items)
	if err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}
	if stored != 3 {
		t.Fatalf("stored = %d, want 3 (templated override should resolve all PKs)", stored)
	}
	if extractFailures != 0 {
		t.Fatalf("extractFailures = %d, want 0", extractFailures)
	}
}

// TestUpsertBatch_GenericFallbackList covers each name in the reduced
// fallback list. The kalshi-accreted names (ticker/event_ticker/series_ticker)
// were dropped because the user owns kalshi and will regenerate
// it with x-resource-id annotations; this test pins what the generic list
// is now responsible for so a future trim doesn't silently break unannotated
// specs.
func TestUpsertBatch_GenericFallbackList(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	for _, key := range []string{"id", "ID", "name", "uuid", "slug", "key", "code", "uid"} {
		t.Run(key, func(t *testing.T) {
			rt := "fallback_" + key
			items := []json.RawMessage{
				json.RawMessage(fmt.Sprintf(`{%q: %q}`, key, "value-1")),
				json.RawMessage(fmt.Sprintf(`{%q: %q}`, key, "value-2")),
			}
			stored, extractFailures, err := s.UpsertBatch(rt, items)
			if err != nil {
				t.Fatalf("UpsertBatch(%q): %v", key, err)
			}
			if stored != 2 {
				t.Fatalf("stored = %d, want 2 (fallback %q must resolve)", stored, key)
			}
			if extractFailures != 0 {
				t.Fatalf("extractFailures = %d, want 0", extractFailures)
			}
		})
	}

	// Negative: API-specific names dropped must NOT resolve.
	// Spec authors annotate these via x-resource-id instead.
	for _, key := range []string{"ticker", "event_ticker", "series_ticker"} {
		t.Run("dropped_"+key, func(t *testing.T) {
			rt := "dropped_" + key
			items := []json.RawMessage{
				json.RawMessage(fmt.Sprintf(`{%q: %q}`, key, "v1")),
			}
			stored, extractFailures, err := s.UpsertBatch(rt, items)
			if err != nil {
				t.Fatalf("UpsertBatch(%q): %v", key, err)
			}
			if stored != 0 {
				t.Fatalf("stored = %d, want 0 (%q must NOT be in the generic fallback list)", stored, key)
			}
			if extractFailures != 1 {
				t.Fatalf("extractFailures = %d, want 1 (%q drop must surface as extract failure)", extractFailures, key)
			}
		})
	}
}

// TestUpsertBatch_ExtractFailuresReturnedForPerItemMisses pins the third
// return value: items that survive JSON unmarshal but have no extractable
// PK (templated override AND generic fallback both miss) bump
// extractFailures. The sync.go.tmpl call site uses this to emit the
// per-resource primary_key_unresolved sync_anomaly the first time silent
// drops occur.
func TestUpsertBatch_ExtractFailuresReturnedForPerItemMisses(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "ok-1"}`),
		json.RawMessage(`{"some_random_field": "no-pk-here"}`),
		json.RawMessage(`{"id": "ok-2"}`),
		json.RawMessage(`{"another_field": 42}`),
	}
	stored, extractFailures, err := s.UpsertBatch("mixed_extraction", items)
	if err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}
	if stored != 2 {
		t.Fatalf("stored = %d, want 2 (only items with id should land)", stored)
	}
	if extractFailures != 2 {
		t.Fatalf("extractFailures = %d, want 2 (two items have no extractable PK)", extractFailures)
	}
}

// TestUpsertBatch_PopulatesActionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed actions table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesActionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("actions", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "actions").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "actions")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count actions: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("actions count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesActionsBoardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed actions_board table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesActionsBoardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("actions_board", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "actions_board").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "actions_board")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count actions_board: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("actions_board count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesActionsCardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed actions_card table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesActionsCardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("actions_card", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "actions_card").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "actions_card")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count actions_card: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("actions_card count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesActionsListTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed actions_list table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesActionsListTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("actions_list", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "actions_list").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "actions_list")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count actions_list: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("actions_list count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesActionsMemberTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed actions_member table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesActionsMemberTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("actions_member", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "actions_member").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "actions_member")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count actions_member: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("actions_member count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesActionsMemberCreatorTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed actions_member_creator table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesActionsMemberCreatorTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("actions_member_creator", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "actions_member_creator").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "actions_member_creator")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count actions_member_creator: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("actions_member_creator count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesActionsOrganizationTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed actions_organization table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesActionsOrganizationTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("actions_organization", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "actions_organization").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "actions_organization")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count actions_organization: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("actions_organization count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesReactionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed reactions table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesReactionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("reactions", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "reactions").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "reactions")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count reactions: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("reactions count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesReactionsSummaryTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed reactions_summary table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesReactionsSummaryTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("reactions_summary", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "reactions_summary").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "reactions_summary")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count reactions_summary: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("reactions_summary count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesTextTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed text table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesTextTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "actions_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "actions_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("text", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "text").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "text")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count text: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("text count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesApplicationsComplianceTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed applications_compliance table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesApplicationsComplianceTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "applications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "applications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "applications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("applications_compliance", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "applications_compliance").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "applications_compliance")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count applications_compliance: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("applications_compliance count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("boards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsActionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_actions table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsActionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_actions", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_actions").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_actions")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_actions: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_actions count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardPluginsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed board_plugins table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardPluginsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("board_plugins", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "board_plugins").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "board_plugins")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count board_plugins: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("board_plugins count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsBoardStarsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_board_stars table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsBoardStarsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_board_stars", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_board_stars").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_board_stars")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_board_stars: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_board_stars count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCalendarKeyTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed calendar_key table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCalendarKeyTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("calendar_key", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "calendar_key").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "calendar_key")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count calendar_key: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("calendar_key count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsCardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_cards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsCardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_cards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_cards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_cards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_cards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_cards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsChecklistsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_checklists table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsChecklistsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_checklists", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_checklists").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_checklists")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_checklists: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_checklists count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsCustomFieldsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_custom_fields table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsCustomFieldsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_custom_fields", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_custom_fields").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_custom_fields")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_custom_fields: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_custom_fields count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesEmailKeyTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed email_key table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesEmailKeyTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("email_key", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "email_key").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "email_key")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count email_key: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("email_key count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsExportsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_exports table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsExportsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_exports", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_exports").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_exports")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_exports: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_exports count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesIdTagsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed id_tags table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesIdTagsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("id_tags", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "id_tags").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "id_tags")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count id_tags: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("id_tags count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsLabelsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_labels table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsLabelsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_labels", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_labels").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_labels")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_labels: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_labels count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsListsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_lists table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsListsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_lists", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_lists").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_lists")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_lists: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_lists count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMarkedAsViewedTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed marked_as_viewed table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMarkedAsViewedTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("marked_as_viewed", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "marked_as_viewed").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "marked_as_viewed")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count marked_as_viewed: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("marked_as_viewed count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsMembersTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_members table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsMembersTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_members", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_members").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_members")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_members: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_members count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsMembershipsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_memberships table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsMembershipsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_memberships", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_memberships").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_memberships")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_memberships: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_memberships count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMyPrefsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed my_prefs table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMyPrefsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("my_prefs", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "my_prefs").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "my_prefs")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count my_prefs: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("my_prefs count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsPluginsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_plugins table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsPluginsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "boards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "boards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_plugins", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_plugins").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_plugins")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_plugins: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_plugins count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("cards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsActionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_actions table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsActionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_actions", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_actions").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_actions")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_actions: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_actions count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesAttachmentsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed attachments table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesAttachmentsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("attachments", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "attachments").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "attachments")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count attachments: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("attachments count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsBoardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_board table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsBoardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_board", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_board").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_board")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_board: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_board count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCheckItemTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed check_item table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCheckItemTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("check_item", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "check_item").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "check_item")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count check_item: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("check_item count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCheckItemStatesTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed check_item_states table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCheckItemStatesTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("check_item_states", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "check_item_states").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "check_item_states")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count check_item_states: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("check_item_states count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesChecklistTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed checklist table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesChecklistTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("checklist", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "checklist").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "checklist")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count checklist: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("checklist count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsChecklistsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_checklists table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsChecklistsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_checklists", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_checklists").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_checklists")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_checklists: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_checklists count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCustomFieldTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed custom_field table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCustomFieldTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("custom_field", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "custom_field").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "custom_field")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count custom_field: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("custom_field count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCustomFieldItemsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed custom_field_items table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCustomFieldItemsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("custom_field_items", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "custom_field_items").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "custom_field_items")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count custom_field_items: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("custom_field_items count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsCustomFieldsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_custom_fields table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsCustomFieldsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_custom_fields", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_custom_fields").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_custom_fields")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_custom_fields: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_custom_fields count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesIdLabelsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed id_labels table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesIdLabelsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("id_labels", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "id_labels").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "id_labels")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count id_labels: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("id_labels count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesIdMembersTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed id_members table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesIdMembersTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("id_members", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "id_members").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "id_members")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count id_members: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("id_members count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsLabelsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_labels table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsLabelsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_labels", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_labels").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_labels")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_labels: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_labels count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsListTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_list table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsListTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_list", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_list").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_list")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_list: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_list count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMarkAssociatedNotificationsReadTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed mark_associated_notifications_read table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMarkAssociatedNotificationsReadTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("mark_associated_notifications_read", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "mark_associated_notifications_read").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "mark_associated_notifications_read")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count mark_associated_notifications_read: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("mark_associated_notifications_read count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsMembersTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_members table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsMembersTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_members", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_members").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_members")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_members: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_members count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersVotedTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_voted table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersVotedTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_voted", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_voted").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_voted")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_voted: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_voted count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCardsPluginDataTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed cards_plugin_data table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCardsPluginDataTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("cards_plugin_data", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "cards_plugin_data").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "cards_plugin_data")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count cards_plugin_data: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("cards_plugin_data count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesStickersTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed stickers table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesStickersTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "cards_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "cards_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("stickers", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "stickers").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "stickers")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count stickers: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("stickers count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesChecklistsBoardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed checklists_board table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesChecklistsBoardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "checklists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "checklists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "checklists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("checklists_board", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "checklists_board").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "checklists_board")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count checklists_board: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("checklists_board count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesChecklistsCardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed checklists_cards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesChecklistsCardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "checklists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "checklists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "checklists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("checklists_cards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "checklists_cards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "checklists_cards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count checklists_cards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("checklists_cards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCheckItemsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed check_items table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCheckItemsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "checklists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "checklists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "checklists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("check_items", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "check_items").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "check_items")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count check_items: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("check_items count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCustomFieldsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed custom_fields table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCustomFieldsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("custom-fields", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "custom-fields").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "custom_fields")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count custom_fields: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("custom_fields count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOptionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed options table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOptionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "custom_fields_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "custom_fields_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "custom_fields_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("options", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "options").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "options")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count options: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("options count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesEnterprisesTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed enterprises table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesEnterprisesTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("enterprises", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "enterprises").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "enterprises")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count enterprises: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("enterprises count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesAdminsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed admins table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesAdminsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("admins", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "admins").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "admins")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count admins: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("admins count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesAuditlogTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed auditlog table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesAuditlogTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("auditlog", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "auditlog").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "auditlog")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count auditlog: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("auditlog count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesClaimableOrganizationsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed claimable_organizations table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesClaimableOrganizationsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("claimable_organizations", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "claimable_organizations").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "claimable_organizations")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count claimable_organizations: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("claimable_organizations count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesEnterprisesMembersTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed enterprises_members table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesEnterprisesMembersTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("enterprises_members", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "enterprises_members").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "enterprises_members")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count enterprises_members: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("enterprises_members count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesEnterprisesOrganizationsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed enterprises_organizations table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesEnterprisesOrganizationsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("enterprises_organizations", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "enterprises_organizations").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "enterprises_organizations")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count enterprises_organizations: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("enterprises_organizations count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesPendingOrganizationsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed pending_organizations table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesPendingOrganizationsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("pending_organizations", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "pending_organizations").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "pending_organizations")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count pending_organizations: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("pending_organizations count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesSignupUrlTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed signup_url table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesSignupUrlTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("signup_url", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "signup_url").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "signup_url")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count signup_url: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("signup_url count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesEnterprisesTokensTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed enterprises_tokens table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesEnterprisesTokensTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("enterprises_tokens", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "enterprises_tokens").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "enterprises_tokens")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count enterprises_tokens: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("enterprises_tokens count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesTransferrableTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed transferrable table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesTransferrableTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "enterprises_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "enterprises_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("transferrable", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "transferrable").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "transferrable")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count transferrable: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("transferrable count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesListsActionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed lists_actions table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesListsActionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "lists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("lists_actions", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "lists_actions").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "lists_actions")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count lists_actions: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("lists_actions count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesArchiveAllCardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed archive_all_cards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesArchiveAllCardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "lists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("archive_all_cards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "archive_all_cards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "archive_all_cards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count archive_all_cards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("archive_all_cards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesListsBoardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed lists_board table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesListsBoardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "lists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("lists_board", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "lists_board").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "lists_board")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count lists_board: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("lists_board count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesListsCardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed lists_cards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesListsCardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "lists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("lists_cards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "lists_cards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "lists_cards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count lists_cards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("lists_cards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesClosedTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed closed table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesClosedTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "lists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("closed", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "closed").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "closed")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count closed: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("closed count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesIdBoardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed id_board table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesIdBoardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "lists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("id_board", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "id_board").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "id_board")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count id_board: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("id_board count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMoveAllCardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed move_all_cards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMoveAllCardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "lists_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "lists_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("move_all_cards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "move_all_cards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "move_all_cards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count move_all_cards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("move_all_cards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersActionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_actions table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersActionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_actions", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_actions").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_actions")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_actions: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_actions count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesAvatarTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed avatar table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesAvatarTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("avatar", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "avatar").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "avatar")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count avatar: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("avatar count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardBackgroundsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed board_backgrounds table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardBackgroundsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("board_backgrounds", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "board_backgrounds").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "board_backgrounds")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count board_backgrounds: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("board_backgrounds count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersBoardStarsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_board_stars table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersBoardStarsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_board_stars", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_board_stars").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_board_stars")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_board_stars: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_board_stars count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersBoardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_boards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersBoardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_boards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_boards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_boards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_boards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_boards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesBoardsInvitedTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed boards_invited table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesBoardsInvitedTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("boards_invited", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "boards_invited").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "boards_invited")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count boards_invited: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("boards_invited count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersCardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_cards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersCardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_cards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_cards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_cards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_cards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_cards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCustomBoardBackgroundsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed custom_board_backgrounds table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCustomBoardBackgroundsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("custom_board_backgrounds", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "custom_board_backgrounds").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "custom_board_backgrounds")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count custom_board_backgrounds: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("custom_board_backgrounds count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCustomEmojiTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed custom_emoji table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCustomEmojiTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("custom_emoji", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "custom_emoji").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "custom_emoji")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count custom_emoji: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("custom_emoji count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesCustomStickersTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed custom_stickers table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesCustomStickersTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("custom_stickers", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "custom_stickers").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "custom_stickers")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count custom_stickers: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("custom_stickers count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersNotificationsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_notifications table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersNotificationsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_notifications", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_notifications").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_notifications")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_notifications: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_notifications count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNotificationsChannelSettingsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed notifications_channel_settings table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNotificationsChannelSettingsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("notifications_channel_settings", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "notifications_channel_settings").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "notifications_channel_settings")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count notifications_channel_settings: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("notifications_channel_settings count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOneTimeMessagesDismissedTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed one_time_messages_dismissed table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOneTimeMessagesDismissedTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("one_time_messages_dismissed", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "one_time_messages_dismissed").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "one_time_messages_dismissed")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count one_time_messages_dismissed: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("one_time_messages_dismissed count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersOrganizationsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_organizations table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersOrganizationsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_organizations", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_organizations").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_organizations")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_organizations: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_organizations count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsInvitedTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations_invited table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsInvitedTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("organizations_invited", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations_invited").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations_invited")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations_invited: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations_invited count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesSavedSearchesTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed saved_searches table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesSavedSearchesTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("saved_searches", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "saved_searches").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "saved_searches")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count saved_searches: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("saved_searches count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesMembersTokensTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed members_tokens table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesMembersTokensTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "members_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "members_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("members_tokens", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "members_tokens").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "members_tokens")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count members_tokens: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("members_tokens count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNotificationsBoardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed notifications_board table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNotificationsBoardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "notifications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("notifications_board", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "notifications_board").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "notifications_board")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count notifications_board: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("notifications_board count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNotificationsCardTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed notifications_card table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNotificationsCardTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "notifications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("notifications_card", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "notifications_card").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "notifications_card")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count notifications_card: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("notifications_card count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNotificationsListTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed notifications_list table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNotificationsListTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "notifications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("notifications_list", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "notifications_list").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "notifications_list")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count notifications_list: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("notifications_list count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNotificationsMemberTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed notifications_member table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNotificationsMemberTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "notifications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("notifications_member", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "notifications_member").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "notifications_member")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count notifications_member: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("notifications_member count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNotificationsMemberCreatorTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed notifications_member_creator table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNotificationsMemberCreatorTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "notifications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("notifications_member_creator", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "notifications_member_creator").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "notifications_member_creator")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count notifications_member_creator: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("notifications_member_creator count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNotificationsOrganizationTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed notifications_organization table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNotificationsOrganizationTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "notifications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("notifications_organization", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "notifications_organization").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "notifications_organization")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count notifications_organization: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("notifications_organization count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesUnreadTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed unread table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesUnreadTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "notifications_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "notifications_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("unread", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "unread").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "unread")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count unread: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("unread count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("organizations", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsActionsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations_actions table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsActionsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("organizations_actions", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations_actions").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations_actions")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations_actions: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations_actions count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsBoardsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations_boards table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsBoardsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("organizations_boards", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations_boards").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations_boards")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations_boards: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations_boards count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsExportsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations_exports table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsExportsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("organizations_exports", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations_exports").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations_exports")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations_exports: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations_exports count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesLogoTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed logo table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesLogoTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("logo", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "logo").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "logo")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count logo: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("logo count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsMembersTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations_members table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsMembersTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("organizations_members", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations_members").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations_members")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations_members: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations_members count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsMembershipsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations_memberships table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsMembershipsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("organizations_memberships", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations_memberships").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations_memberships")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations_memberships: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations_memberships count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesNewBillableGuestsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed new_billable_guests table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesNewBillableGuestsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("new_billable_guests", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "new_billable_guests").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "new_billable_guests")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count new_billable_guests: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("new_billable_guests count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesOrganizationsPluginDataTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed organizations_plugin_data table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesOrganizationsPluginDataTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("organizations_plugin_data", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "organizations_plugin_data").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "organizations_plugin_data")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count organizations_plugin_data: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("organizations_plugin_data count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesPrefsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed prefs table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesPrefsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("prefs", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "prefs").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "prefs")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count prefs: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("prefs count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesTagsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed tags table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesTagsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "organizations_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "organizations_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("tags", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "tags").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "tags")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count tags: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("tags count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesPluginsComplianceTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed plugins_compliance table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesPluginsComplianceTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "plugins_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "plugins_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "plugins_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("plugins_compliance", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "plugins_compliance").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "plugins_compliance")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count plugins_compliance: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("plugins_compliance count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesListingTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed listing table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesListingTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "plugins_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "plugins_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "plugins_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("listing", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "listing").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "listing")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count listing: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("listing count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesListingsTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed listings table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesListingsTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "plugins_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "plugins_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "plugins_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("listings", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "listings").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "listings")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count listings: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("listings count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesTokensTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed tokens table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesTokensTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("tokens", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "tokens").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "tokens")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count tokens: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("tokens count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesTokensMemberTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed tokens_member table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesTokensMemberTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "tokens_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "tokens_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "tokens_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("tokens_member", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "tokens_member").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "tokens_member")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count tokens_member: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("tokens_member count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesTokensWebhooksTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed tokens_webhooks table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesTokensWebhooksTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001", "tokens_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-002", "tokens_id": "test-parent-001"}`),
		json.RawMessage(`{"id": "test-003", "tokens_id": "test-parent-001"}`),
	}
	if _, _, err := s.UpsertBatch("tokens_webhooks", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "tokens_webhooks").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "tokens_webhooks")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count tokens_webhooks: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("tokens_webhooks count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesTrelloCalendarSearchTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed trello_calendar_search table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesTrelloCalendarSearchTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("trello-calendar-search", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "trello-calendar-search").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "trello_calendar_search")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count trello_calendar_search: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("trello_calendar_search count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}

// TestUpsertBatch_PopulatesWebhooksTable verifies that UpsertBatch
// dispatches paginated items into both the generic resources table AND the
// typed webhooks table. Regression for issue #268: before the fix, paginated
// syncs only filled the generic resources table, so domain commands that
// query the typed table saw zero rows.
func TestUpsertBatch_PopulatesWebhooksTable(t *testing.T) {
	dbPath := filepath.Join(t.TempDir(), "data.db")
	s, err := Open(dbPath)
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	defer s.Close()

	items := []json.RawMessage{
		json.RawMessage(`{"id": "test-001"}`),
		json.RawMessage(`{"id": "test-002"}`),
		json.RawMessage(`{"id": "test-003"}`),
	}
	if _, _, err := s.UpsertBatch("webhooks", items); err != nil {
		t.Fatalf("UpsertBatch: %v", err)
	}

	db := s.DB()

	var generic int
	if err := db.QueryRow(`SELECT COUNT(*) FROM resources WHERE resource_type = ?`, "webhooks").Scan(&generic); err != nil {
		t.Fatalf("count resources: %v", err)
	}
	if generic != len(items) {
		t.Fatalf("resources count = %d, want %d", generic, len(items))
	}

	var typed int
	typedQuery := fmt.Sprintf(`SELECT COUNT(*) FROM "%s"`, "webhooks")
	if err := db.QueryRow(typedQuery).Scan(&typed); err != nil {
		t.Fatalf("count webhooks: %v", err)
	}
	if typed != len(items) {
		t.Fatalf("webhooks count = %d, want %d (typed table not populated by UpsertBatch)", typed, len(items))
	}
}
