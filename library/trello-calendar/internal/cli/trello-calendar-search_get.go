// Copyright 2026 peter-moelzer. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTrelloCalendarSearchGetCmd(flags *rootFlags) *cobra.Command {
	var flagQuery string
	var flagIdBoards string
	var flagIdOrganizations string
	var flagIdCards string
	var flagModelTypes string
	var flagBoardFields string
	var flagBoardsLimit int
	var flagBoardOrganization bool
	var flagCardFields string
	var flagCardsLimit int
	var flagCardsPage float64
	var flagCardBoard bool
	var flagCardList bool
	var flagCardMembers bool
	var flagCardStickers bool
	var flagCardAttachments string
	var flagOrganizationFields string
	var flagOrganizationsLimit int
	var flagMemberFields string
	var flagMembersLimit int
	var flagPartial bool

	cmd := &cobra.Command{
		Use:         "get",
		Aliases:     []string{"list"},
		Short:       "Find what you're looking for in Trello",
		Example:     "  trello-calendar-pp-cli trello-calendar-search get --query example-value",
		Annotations: map[string]string{"pp:endpoint": "trello-calendar-search.get", "pp:method": "GET", "pp:path": "/search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("query") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "query")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/search"
			params := map[string]string{}
			if flagQuery != "" {
				params["query"] = fmt.Sprintf("%v", flagQuery)
			}
			if flagIdBoards != "" {
				params["idBoards"] = fmt.Sprintf("%v", flagIdBoards)
			}
			if flagIdOrganizations != "" {
				params["idOrganizations"] = fmt.Sprintf("%v", flagIdOrganizations)
			}
			if flagIdCards != "" {
				params["idCards"] = fmt.Sprintf("%v", flagIdCards)
			}
			if flagModelTypes != "" {
				params["modelTypes"] = fmt.Sprintf("%v", flagModelTypes)
			}
			if flagBoardFields != "" {
				params["board_fields"] = fmt.Sprintf("%v", flagBoardFields)
			}
			if flagBoardsLimit != 0 {
				params["boards_limit"] = fmt.Sprintf("%v", flagBoardsLimit)
			}
			if flagBoardOrganization != false {
				params["board_organization"] = fmt.Sprintf("%v", flagBoardOrganization)
			}
			if flagCardFields != "" {
				params["card_fields"] = fmt.Sprintf("%v", flagCardFields)
			}
			if flagCardsLimit != 0 {
				params["cards_limit"] = fmt.Sprintf("%v", flagCardsLimit)
			}
			if flagCardsPage != 0.0 {
				params["cards_page"] = fmt.Sprintf("%v", flagCardsPage)
			}
			if flagCardBoard != false {
				params["card_board"] = fmt.Sprintf("%v", flagCardBoard)
			}
			if flagCardList != false {
				params["card_list"] = fmt.Sprintf("%v", flagCardList)
			}
			if flagCardMembers != false {
				params["card_members"] = fmt.Sprintf("%v", flagCardMembers)
			}
			if flagCardStickers != false {
				params["card_stickers"] = fmt.Sprintf("%v", flagCardStickers)
			}
			if flagCardAttachments != "" {
				params["card_attachments"] = fmt.Sprintf("%v", flagCardAttachments)
			}
			if flagOrganizationFields != "" {
				params["organization_fields"] = fmt.Sprintf("%v", flagOrganizationFields)
			}
			if flagOrganizationsLimit != 0 {
				params["organizations_limit"] = fmt.Sprintf("%v", flagOrganizationsLimit)
			}
			if flagMemberFields != "" {
				params["member_fields"] = fmt.Sprintf("%v", flagMemberFields)
			}
			if flagMembersLimit != 0 {
				params["members_limit"] = fmt.Sprintf("%v", flagMembersLimit)
			}
			if flagPartial != false {
				params["partial"] = fmt.Sprintf("%v", flagPartial)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "trello-calendar-search", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagQuery, "query", "", "The search query with a length of 1 to 16384 characters")
	cmd.Flags().StringVar(&flagIdBoards, "id-boards", "", "`mine` or a comma-separated list of Board IDs")
	cmd.Flags().StringVar(&flagIdOrganizations, "id-organizations", "", "A comma-separated list of Organization IDs")
	cmd.Flags().StringVar(&flagIdCards, "id-cards", "", "A comma-separated list of Card IDs")
	cmd.Flags().StringVar(&flagModelTypes, "model-types", "all", "What type or types of Trello objects you want to search. all or a comma-separated list of: `actions`, `boards`,...")
	cmd.Flags().StringVar(&flagBoardFields, "board-fields", "name,idOrganization", "all or a comma-separated list of: `closed`, `dateLastActivity`, `dateLastView`, `desc`, `descData`,...")
	cmd.Flags().IntVar(&flagBoardsLimit, "boards-limit", 10, "The maximum number of boards returned. Maximum: 1000")
	cmd.Flags().BoolVar(&flagBoardOrganization, "board-organization", false, "Whether to include the parent organization with board results")
	cmd.Flags().StringVar(&flagCardFields, "card-fields", "all", "all or a comma-separated list of: `badges`, `checkItemStates`, `closed`, `dateLastActivity`, `desc`, `descData`,...")
	cmd.Flags().IntVar(&flagCardsLimit, "cards-limit", 10, "The maximum number of cards to return. Maximum: 1000")
	cmd.Flags().Float64Var(&flagCardsPage, "cards-page", 0.000000, "The page of results for cards. Maximum: 100")
	cmd.Flags().BoolVar(&flagCardBoard, "card-board", false, "Whether to include the parent board with card results")
	cmd.Flags().BoolVar(&flagCardList, "card-list", false, "Whether to include the parent list with card results")
	cmd.Flags().BoolVar(&flagCardMembers, "card-members", false, "Whether to include member objects with card results")
	cmd.Flags().BoolVar(&flagCardStickers, "card-stickers", false, "Whether to include sticker objects with card results")
	cmd.Flags().StringVar(&flagCardAttachments, "card-attachments", "false", "Whether to include attachment objects with card results. A boolean value (true or false) or cover for only card...")
	cmd.Flags().StringVar(&flagOrganizationFields, "organization-fields", "name,displayName", "all or a comma-separated list of billableMemberCount, desc, descData, displayName, idBoards, invitations, invited,...")
	cmd.Flags().IntVar(&flagOrganizationsLimit, "organizations-limit", 0, "The maximum number of Workspaces to return. Maximum 1000")
	cmd.Flags().StringVar(&flagMemberFields, "member-fields", "avatarHash,fullName,initials,username,confirmed", "all or a comma-separated list of: avatarHash, bio, bioData, confirmed, fullName, idPremOrgsAdmin, initials,...")
	cmd.Flags().IntVar(&flagMembersLimit, "members-limit", 0, "The maximum number of members to return. Maximum 1000")
	cmd.Flags().BoolVar(&flagPartial, "partial", false, "By default, Trello searches for each word in your query against exactly matching words within Member content....")

	return cmd
}
