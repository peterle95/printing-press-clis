version: 2
project_name: englishjobs-pp-cli
changelog:
  disable: true
builds:
  - id: englishjobs-pp-cli
    main: ./cmd/englishjobs-pp-cli
    binary: englishjobs-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X englishjobs-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: englishjobs-pp-mcp
    main: ./cmd/englishjobs-pp-mcp
    binary: englishjobs-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: englishjobs-pp-cli
    repository:
      owner: peter-moelzer
      name: homebrew-tap
    homepage: "https://github.com/peter-moelzer/englishjobs-pp-cli"
    description: "Public EnglishJobs.de HTML job search pages."
    install: |
      bin.install "englishjobs-pp-cli"
      bin.install "englishjobs-pp-mcp"
