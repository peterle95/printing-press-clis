version: 2
project_name: stepstone-pp-cli
changelog:
  disable: true
builds:
  - id: stepstone-pp-cli
    main: ./cmd/stepstone-pp-cli
    binary: stepstone-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X stepstone-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: stepstone-pp-mcp
    main: ./cmd/stepstone-pp-mcp
    binary: stepstone-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: stepstone-pp-cli
    repository:
      owner: peter-moelzer
      name: homebrew-tap
    homepage: "https://github.com/peter-moelzer/stepstone-pp-cli"
    description: "Public StepStone HTML job search pages."
    install: |
      bin.install "stepstone-pp-cli"
      bin.install "stepstone-pp-mcp"
