version: 2
project_name: linkedin-pp-cli
changelog:
  disable: true
builds:
  - id: linkedin-pp-cli
    main: ./cmd/linkedin-pp-cli
    binary: linkedin-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X linkedin-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: linkedin-pp-mcp
    main: ./cmd/linkedin-pp-mcp
    binary: linkedin-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: linkedin-pp-cli
    repository:
      owner: peter-moelzer
      name: homebrew-tap
    homepage: "https://github.com/peter-moelzer/linkedin-pp-cli"
    description: "LinkedIn robots/surface checker. Automated job scraping is not enabled without permission."
    install: |
      bin.install "linkedin-pp-cli"
      bin.install "linkedin-pp-mcp"
